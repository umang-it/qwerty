{
    "name": "@servicenow/macbro",
    "version": "1.0.0",
    "description": "Application containing a single script called add.",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.12.1",
        "@servicenow/glide": "27.0.5"
    }
}
